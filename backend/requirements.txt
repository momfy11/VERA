# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0

# Database
sqlalchemy>=2.0.36
alembic>=1.13.0
psycopg2-binary>=2.9.10
asyncpg>=0.30.0

# Config & validation
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.1

# Security
python-multipart>=0.0.18
cryptography>=44.0.0

# Scheduling (Sprint 5)
apscheduler>=3.10.4

# Observability
structlog>=24.4.0

# LLM providers — supports groq, gemini (via openai SDK), ollama (via openai SDK), mistral
mistralai>=1.2.0
groq>=0.11.0
openai>=1.50.0
httpx>=0.27.0

# Tool calling — web search, system
ddgs>=9.0.0
plyer>=2.1.0
pyperclip>=1.8.0

# Phase 5.1 — semantic memory (embeddings + pgvector)
fastembed>=0.4.0
pgvector>=0.3.0
